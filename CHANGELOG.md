# 変更履歴

このプロジェクトの主な変更をまとめます。書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に概ね従います。

バージョンの上げ方は [docs](README.md) ではなく下記の運用に従ってください:
`npm run bump:minor`（または `bump:patch` / `bump:major`）で `package.json` の番号だけを更新し、
変更を PR に含めて main へマージすると、CI が自動で `vX.Y.Z` タグと GitHub Release を作成します。

## [Unreleased]

## [1.6.0] - 2026-07-21

**やりきった実感と、次の一手。** 構文ドリル・意味ノード生成を **10問=1セット**の区切りに変え、
セット完了に演出を付けた。ゴールの青枠には「次のステップに進むには何を何回やればいいか」を
具体的に出す。あわせてホームの数値パネルを整理し、件数は一覧タイトルへ移した。

### 追加
- **セット制ドリルとセット完了演出**（`hooks/useSession.ts`・`components/SessionSummary.tsx`）。
  🧱 構文ドリルと 🧠 意味ノード生成が **10枚=1セット**（`DRILL_SET_SIZE`）のランダム出題になり、
  セットを終えると **紙吹雪＋ポップイン・「セット N 完了！」・正答率・全問正解の 🏆・「次のセットへ →」**
  を出す。`prefers-reduced-motion` では演出を止め、同じ情報を静止画面で見せる。
  `useSession` に `setSize`（1セットの枚数）と `requeueWeak`（弱いカードを列末尾へ積み直すか）を追加
- **意味ノード英語思考の STEP1・STEP2 に日本語の音声入力**（`hooks/useSpeechInput.ts`・
  `components/VoiceInput.tsx`・`lib/speechText.ts`）。Web Speech API によるディクテーションで、
  STEP1（思考の自由記述）は空白区切り、STEP2（意味ノード）は **1発話=1ノード（1行）** として追記する。
  対応していない端末ではボタンを出さない（Android の WebView では使えないことが多い）
- **ゴールの青枠に「次のステップに進むには」**（`lib/goals.ts` の `nextActions()`・
  `components/GoalProgress.tsx`）。`0/12` が何をすれば進むのかを、指標ごとに具体的な回数へ翻訳する。
  例:「🧱 構文ドリルで「できた」をあと 18 回」「🧱 構文ドリルで5秒以内の起動をあと 7 回」
  「💬 チャット練習をあと 4 セッション」。タップでそのモードへ直行できる

### 変更
- **構文ドリルの出題順をランダムに**（`clusterByFacet` を外した）。セット制と噛み合わないため
- **英文を一度でも開示すれば採点できる**ようにした（構文ドリル・意味ノード生成）。従来は
  カードの最後の例文まで開示しないと採点ボタンが出ず、途中で送ると「✕ やめる」しか出口が無かった
- **セットの枚数を必ず揃える**。期日到来カードが `setSize` に満たないときは、残りをランダムに補充する
- **チャンク一覧・例文一覧のタイトルに登録件数**を出す（「チャンク一覧（443件登録）」）。
  絞り込み中のみ「絞り込み N 件」を併記する（`usePhraseFilter` が母数 `base` を公開）

### 削除
- **ホームの数値パネル3枚**（今日のあゆみ／今日の出題・新規・習得済み／今日やった・登録フレーズ）。
  ホームの主役をゴールと今日のプランに戻す。登録件数は各一覧のタイトルへ移した

## [1.5.0] - 2026-07-20

**ゴール梯子を「構造優先」に改訂。** 学習診断の結論＝ボトルネックはチャンクの「量」ではなく
構造の「自動化」を踏まえ、序盤の中間ゴールに**構文の反射化と英語思考の起動速度**を据えた。
「量」から「速さ」への KPI 転換。加えて、**自分の思考**を素材に英語で考える新練習
「💭 意味ノード英語思考」を追加した。

### 追加
- **新練習モード「💭 意味ノード英語思考」**（`/think`・`pages/ThinkPractice.tsx`）。用意されたカードを
  練習する「🧠 意味ノード生成」とは別に、**自分自身の思考**を出発点に「日本語で完成文を作ってから
  訳す」癖を断つ5ステップのウィザード:
  1. 思考を日本語で自由記述 → 2. 自分で意味ノードに分解 → 3. 自分で英文化 → 4. AI英文チェック → 5. カード保存
- **Gemma（ユーザーの Gemini APIキー・既存 `chatApi`）を2回呼び出す**（`lib/thinkPrompt.ts`・純関数として分離）。
  ①分解評価（1ノード=1機能・骨子か・結論先出しか・欠落・ノード数2〜5を講評＋AIの推奨分解）、
  ②英文チェック（平易・シンプル・ビジネスで通用するかを重視し、学習者の文を活かした**最小修正**を提案）。
  応答は `enrich.ts` と同じく固定スキーマJSONで返させ、`aiJson.extractJson` でパース（失敗時は再試行）
- **Message カード（`type='Message'`）として保存**すると、既存の意味ノード生成ドリルの**SRSに乗って再学習**できる
  （id=`think-…`、ja=最終ノード列、en=最終英文列、category=AIが判定した場面名、`level='Core'`）。
  気に入った表現は**表現ストック**へワンタップ保存（チャット練習と共通の `useStock`）
- **学習ログに `think` イベントを追加**（`ThinkEvent { nodeCount, sentenceCount, savedCard }`）。英文チェック
  完了で1本として記録し、`noteChatComplete` と同じ `logAndMaybeStreak` 経由で**当日の最低ライン達成**に数える。
  KPI のアウトプット数（`outputCount`）に組み立てた英文数を加算
- ロジック（プロンプト組み立て・パース・ノード整形）に vitest 16件（正常系・コードフェンス・欠損フィールド・不正JSON）

### 変更
- **全3トラックのゴール梯子を再構成**（`lib/goals.ts` の `GOAL_TRACKS`）。序盤に
  **構文の反射化**（`retainedStructures`＝定着した構文カード数）と**起動レイテンシ**
  （`fastLaunchPct`＝直近7日で和訳→英文起動が threshold 以内だった割合）を置き、
  チャンク量（`retainedChunks`）は土台として中盤に据えた
  - 💼 ビジネス（6段）: 構文を反射→5秒で起動→チャンク50→3秒に短縮→会話で使える→ビジネス領域
  - 🎓 留学（5段）: 構文10枚→5秒で起動→チャンク50→チャット10→習得100
  - 🗣️ 日常（4段）: 構文8枚→5秒で起動→チャンク30→雑談
  - **「累計アウトプット数」の段を撤去**（`totalOutputs`＝量ベースの KPI を梯子から外す）
- **新メトリクス2種**（`GoalMetricKind`）: `retainedStructures`（`type='Structure'` の定着数）・
  `fastLaunchPct`（`GradeEvent.latencyMs` を材料に直近7日で算出。`GoalMetric.thresholdMs` 追加）
- **`retainedChunks` / `masteredChunks` を純チャンク数に修正**。Sentence Engine（構文・意味ノード）と
  長文音読のカードを id で除外し、構文カード等の混入を防いだ

## [1.4.0] - 2026-07-20

**意味ノード50場面への拡充と、Sentence Engine デッキの別系統インポート/エクスポート。**
構文カード・意味ノードカードを、チャンク（Notion往復用CSV）とは**別データ**として
取り込み・書き出しできるようにした。

### 追加
- **内蔵デッキを66枚に拡充**（`public/data/sentence-engine.json`＝構文16・意味ノード**50**）。
  意味ノードは12→50場面へ増やし、ビジネス・マーケティング中心に**38場面を追加**
- **Sentence Engine デッキの別系統インポート/エクスポート**（設定「🧱 Sentence Engine デッキ
  （構文・意味ノード）」）。取込は `type` が `Structure`／`Message` の行だけを受理する
  マージ（ID一致は上書き・SRS進捗は保持）。CSVの列はチャンクCSVと同一で、判別は `Type` 列で行う
  - **📥 CSVを取り込む**: `filterSentenceEngineImport`（純関数）で Structure/Message 以外を除外。
    Structure は en/ja の揃った例文が1件以上必要、Message は examples を空配列に正規化
  - **📤 CSVを書き出す**: Sentence Engine カードだけを `phrasesToCsv` で出力（`csvFilename('sentence-engine')`）
  - **📦 内蔵デッキを取込・更新（構文16・意味ノード50）**: 内蔵の見本デッキを取込。旧28枚版
    （構文16・意味ノード12）から50場面版への**更新導線**にもなる

### 変更
- **チャンクCSVの書き出しから Sentence Engine 教材を除外**（`excludeSentenceEngine`）。
  構文・意味ノードの教材が Notion 往復用CSVに混ざらないようにした
- `SeedDeckEmpty`（内蔵デッキの空表示）の取込ロジックを `importBuiltinSentenceEngine` に一本化
  （文言も「構文16・意味ノード50」に更新。挙動は不変＝取込後そのまま出題）
- ユニットテスト拡充（`filterSentenceEngineImport` の受理／型・examples 検証、seed 66枚検証。全240件）

## [1.3.0] - 2026-07-20

**Sentence Engine（三層学習構造）— 学習OS化フェーズ2。** 「単語→文」ではなく
**チャンク → 構文 → 意味ノード**の三層で英作文を積み上げる。狙いは、日本語で完成文を
作ってから訳す癖を断ち、**和訳・意味の骨子を見た瞬間に英語の骨格へ飛び込む反射**を鍛えること。
その反応速度を**起動レイテンシ**として測り、5秒→3秒を目標に可視化する。

### 追加
- **🧱 構文ドリル**（`/structure`・`pages/StructureDrill.tsx`）。一つの構文パターン
  （例 `X means that Y.` `What matters is X.`）を常時表示しながら、その変形例を
  「日本語 → 声に出して英作文 → タッチで答え合わせ」で連続反復し、型を反射化する
- **🧠 意味ノード生成**（`/message`・`pages/MessageDrill.tsx`）。主張→根拠→補足の
  意味の骨子（改行区切りのノード）を見て、**日本語の完成文を作らず**ノードごとに主語と動詞を
  置いて英文を組み立てる。参考出力は開示のみ（正解は一つではない）
- **内蔵デッキ**（`public/data/sentence-engine.json`＝構文16・意味ノード12）。デッキが空なら
  ワンタップで取り込み、そのまま出題が始まる（共通 `components/SeedDeckEmpty.tsx`）
- **起動レイテンシKPI**（Clause launch latency）。和訳表示→英文開示の中央値を採点ログ
  （`GradeEvent.latencyMs`）に残し、`kpi.launchLatencyMedian` で集計。セッション完了画面に
  「⚡ 起動 X.X秒」（◎3秒以内／○5秒以内）、ホーム「今日のあゆみ」に当日の中央値を表示
- Sentence Engine の判定・内蔵デッキ読込・レイテンシ計測器は純関数 `lib/sentenceEngine.ts`

### 変更
- **今日のプランを「構造優先」に再編**（`lib/planEngine.ts`）。普通 = 🧱 構文ドリル → ▶ 今日の練習、
  余裕あり = 🧱 構文ドリル → ▶ 今日の練習 → 🧠 意味ノード生成 →（キーあれば）💬 チャット。
  疲れてる（🎧 流し聞きのみ）は不変
- `ReproCard` に `speakJa`（和訳のTTS読み上げ可否）・`revealNote`（開示英文の注記）を追加し、
  和訳・英文とも改行ノードを行表示（後方互換）。採点 `mode` に `structure`／`message` を追加
- 構文・意味ノードカードは通常の練習プール・一覧・集計から除外し、専用モードでのみ出題する
- ユニットテスト拡充（`planEngine.test.ts`／`kpi.test.ts`。全235件）

### 備考
- スピーキングチャレンジ（音声認識判定）・構造再利用KPI・AIによる変形/ノード自動生成は将来版。
  本版は自己採点＋起動レイテンシと手動キュレーションの内蔵デッキで代替

## [1.2.1] - 2026-07-20

**ネイティブアプリの画面オフ再生が30〜60秒で止まる問題の修正。**

画面オフでも再生が続くはずのフォアグラウンドサービス（FGS + PARTIAL_WAKE_LOCK）は
プロセスとCPUを生かすが、それだけでは足りなかった。**音を鳴らしているのは Android の
システムTTS（別プロセス）で WebView 自身は無音**のため、画面オフの WebView は
「非表示かつ無音のページ」として Chromium に凍結され、再生を次に進める仕組み
（発話完了コールバック → `setTimeout` の連鎖）ごと止まっていた。画面を点けると
凍結が解けて再開する、という症状はこれで説明がつく。

### 修正
- ネイティブアプリでも**キープアライブ音声（30秒・ほぼ聞こえないWAVのループ）を FGS と併用**
  （`lib/backgroundSession.ts`）。WebView のページを「音声再生中」にしておくことで
  Chromium の凍結・タイマー抑制の対象外にする。v0.11 で Web 向けに実装した仕組みの再利用だが、
  Web での敗因（画面オフで speechSynthesis 自体が止められる）はネイティブTTSには無いため、
  ここでは本来の狙いどおりに効くことを期待した構成
- キープアライブに**ネイティブ用の2オプション**を追加（`lib/keepAlive.ts`）。
  `resilient`: 音声フォーカス喪失などで止められても再生全体は止めず鳴らし直す（上限10回）。
  `useMediaSession`: ネイティブでは FGS の通知が同じ役目を持つので Media Session を使わない
- **レンダラプロセスの優先度**を、WebView 非表示時も落とさないよう明示
  （`MainActivity.java` / `setRendererPriorityPolicy(RENDERER_PRIORITY_IMPORTANT, false)`）

## [1.2.0] - 2026-07-19

**「今日のプラン」— 学習OS化フェーズ1（Plan）。** 「今日は何をどれだけやるか」をアプリが提案し、
決断疲れ＝開始摩擦をなくす。強制はしない（提案・スキップ可）。

### 追加
- **ホームに「今日のプラン」カード**（`components/TodayPlan.tsx` + `lib/planEngine.ts`）。
  まず**体調を3択**（😴 疲れてる / 🙂 普通 / 💪 余裕あり）で選ぶと、当日メニューを自動編成する:
  - 疲れてる → 🎧 連続再生で流し聞き（5分でも最低ライン達成）
  - 普通 → ▶ 今日の練習（期日の枚数を文面に反映）
  - 余裕あり → ▶ 今日の練習 ＋ 💬 チャット練習（APIキー未設定時は ⚡ 瞬間英作文に自動置換）
- 各項目はタップで直行。**今日すでに実施済みの項目は学習ログから自動でチェック（✅）**され、
  採点は mode 別に判定するため「今日の練習だけやると瞬間英作文は未達成のまま」正しく区別する
- プランの選択は当日限りで端末内に保存し、**日付が変われば自動でリセット**（翌朝また体調から）。
  「気分を変える」でいつでも選び直せる
- 編成ルールは純関数 `lib/planEngine.ts`（テスト対象・UIから独立）。ユニットテスト +8（全216件）

### 備考
- 前日の結果に応じた初期値の自動反映（Act）は v1.4.0 で対応予定。本版では毎朝ニュートラルに体調を尋ねる
- 通知（プランへの入口）は v1.3.0 で対応予定

## [1.1.3] - 2026-07-18

### 修正
- **ゴールの中間目標のしきい値を実デッキ（488チャンク）に合わせて較正**。ビジネスの最終段が
  「Work分野の定着80個」だったが Work カテゴリは実データに23件しかなく**到達不能**だったため、
  デッキ最大のビジネス系カテゴリ **Business transformation（29件）で定着20個**に変更（`biz-work`→`biz-domain`）。
  `retainedInCategory` の target はカテゴリ実在数を超えないという不変条件をコメントで明記

## [1.1.2] - 2026-07-18

**デザイン全面刷新: IBM Carbon Design System 準拠。** ルートに置いたデザイントークン定義
`DESIGN.md`（Carbon 抽出）に従い、全27画面・コンポーネントを張り替えた。

### 変更
- **見た目の言語を Carbon に統一**: 白キャンバス＋チャコール文字＋**IBM Blue（#0f62fe）単一アクセント**。
  角丸なし（全要素 0px）・ドロップシャドウなし・**1pxヘアライン境界**と面の切替（白⇔#f4f4f4）で
  階層を表現。ダークは Gray-100 テーマ相当（canvas #161616 / カード #262626）
- **フォントを IBM Plex Sans に**（@fontsource で自己ホスト・オフライン動作は維持。300/400/600）。
  大きな数字・見出しは **weight 300**（Carbonのブランドシグネチャ）、本文は 400＋`letter-spacing: 0.16px`。
  日本語はシステムフォント（Hiragino/Meiryo）にフォールバック
- **共通レシピを CSS コンポーネント層に一元化**（`index.css`: `.tile` `.btn-primary` `.btn-secondary`
  `.btn-tertiary` `.btn-danger` `.chip` `.chip-active` `.input` `.link` `.t-muted` 等）。
  今後の画面追加はこのクラスを使えば自動的にデザインシステムに乗る
- ホームのモード一覧は色とりどりのカードをやめ、**白タイル＋青矢印**の product-card 方式に。
  主導線「▶ 今日の練習」とゴール進捗バーだけが青ソリッド（アクセントの節約）
- 採点ボタン・ステータス表示はセマンティック3色（success 緑 / warning 黄〈ink文字〉/ error 赤）に整理。
  Word Spark ハイライトは青系に変更
- 下部ナビは Carbon タブ様式（選択タブに**上辺2pxの青バー**＋ink文字）

### 備考
- デザイントークンの一次資料はルートの `DESIGN.md`。変更時はここを起点にする
- 機能・挙動の変更なし（テスト208件パス）。学習データにも影響なし

## [1.1.1] - 2026-07-18

v1.1.0 リリース後に確定した修正とドキュメント反映のパッチ（マージ済みPRへ後追いpushとなり
mainに未収載だった2コミットを収録）。

### 修正
- **チャット練習を完了してもストリークが更新されなかった問題**。まとめ完了を1セッションとして
  `useDeck.noteChatComplete` に集約し、当日の最低ライン達成で `bumpStreak` を呼ぶ
  （連続再生の `notePlayback` と共通の `logAndMaybeStreak`）。1メッセージだけ送って
  離脱した会話は記録せず、完了したセッションのみを継続の材料にする（要件FR-33と実装を統一）

### 変更
- ドキュメント一式に v1.1.0 を反映（仕様書 v1.11 / 要件定義書 v1.15 / README / CHANGELOG）。
  要件定義書は FR-169b を v1.0.1 の通常レイアウトに更新し、v1.0.1 改訂履歴と FR-216
  （Android戻るボタン制御）を追補

## [1.1.0] - 2026-07-18

学習エンジンを「日常の時間軸」に載せる第一歩。**学習ログ基盤＋ゴール設定**（学習OS化フェーズ0）を追加し、
ネイティブアプリで CSV・バックアップが保存できなかった不具合を修正する。

### 追加
- **学習ログ基盤**（IndexedDB v3・追記型 `events` ストア。`lib/events.ts` / `lib/kpi.ts`）。
  採点・チャット・連続再生・教材化を「いつ・何を・どれだけ」記録する。既存ユーザーは追加型マイグレーションで
  自動移行（既存データはそのまま）。採点は全モード共通の窓口 `useDeck.grade` で一元記録し、box遷移から
  **「定着（box≧4）への昇格」も検出**。チャットは**まとめ完了時に使えたチャンク（usedChunkIds）を永続化**
- **ゴール設定**（`lib/goals.ts`）。💼ビジネス / 🎓留学 / 🗣️日常会話 の3プリセットから最終ゴール（北極星）を選び、
  そこへ向かう**中間ゴールの梯子**を一つずつクリアする。ホーム一等地に**進捗バー**（達成率・次のゴール・
  タップでステップ一覧）を表示
- **ホームに「今日のあゆみ」**（採点数・アウトプット数・定着数の3指標＋最低ライン達成表示）
- **最低ライン方式**（ゼロの日を作らせない）。採点・チャットが無い日でも**連続再生5分でストリークを維持**
  （`lib/playTracker.ts`：フレーズ連続再生・長文音読の経過秒数を60秒ごと＋停止時に記録）
- ユニットテスト: `kpi.test.ts` +14 / `goals.test.ts` +10 / `playTracker.test.ts` +5（全207件）

### 修正
- **ネイティブアプリでCSVエクスポート・フルバックアップが保存されない問題**。Capacitor WebView では
  ブラウザのダウンロード（`<a download>`）も共有シート（`navigator.share({files})`）も機能しないため、
  `@capacitor/filesystem` で**公開 Documents フォルダへ直接保存**するように変更（Filesアプリ→Documents から
  取り出せる。Android 11+ ではアプリからダウンロードフォルダへ直接書き込めない仕様のため Documents を採用）。
  Web/PWA は従来どおり共有シート→ダウンロード

### 備考
- フルバックアップ（JSON）に学習ログ（`events`）と選択中ゴールを追加。**旧バックアップも従来どおり復元可能**
  （後方互換）。ネイティブ移行・機種変更時は学習ログごと持ち運べる
- ゴールの中間目標のしきい値は暫定値。実際の学習データの分布に合わせて今後調整予定

## [1.0.1] - 2026-07-17

ネイティブアプリの実使用フィードバック対応パッチ。

### 変更
- **チャット練習に下部メニューを復活**（全画面レイアウトを廃止し他タブと統一）。タブの1つなのに
  チャットだけメニューが消える違和感を解消。入力バーはメニューの上に収まり、キーボード表示中も
  見えたまま（メッセージの自動スクロールは従来どおり）

### 修正
- **Androidの戻るボタンでアプリが終了してしまう問題**（`@capacitor/app` の backButton ハンドラを追加）。
  ホーム以外では「前の画面へ戻る（履歴が無ければホームへ）」、ホームでは終了ではなく
  **最小化**（バックグラウンドへ回り、次回起動が速い）になる


## [1.0.0] - 2026-07-17

**Androidネイティブアプリ化。** 当初からの懸案だった「画面オフでの連続再生」を、ハックではなく
Android公式の仕組み（フォアグラウンドサービス）で正式サポートする。Web/PWA版も従来どおり併存。

### 追加
- **Androidアプリ（APK）**（Capacitor 8）。読み上げは Android システムTTS 直結の新エンジン
  （`lib/tts/capacitorTts.ts`。WebViewに speechSynthesis が無いため。Chromeと同じエンジン・声）。
  Word Spark は onRangeStart イベントで**ネイティブでは正確なハイライト**に
- **画面オフでも連続再生・長文音読が継続**（`PlaybackService`: mediaPlayback型フォアグラウンドサービス
  + 部分ウェイクロック。通知に再生中チャンクを表示・カード送りで更新・停止で消灯。
  Android 13+ の通知権限は拒否でも再生可）。実機で確認済み
- **進捗込みフルバックアップ（JSON）**（`lib/backup.ts` + 設定→データ）。教材＋SRS進捗＋ストリークを
  丸ごとエクスポート/復元（全置換・確認付き）。PWA→アプリ移行・機種変更用。APIキーは含めない
- **APKの自動ビルド・署名・Release添付**（`.github/workflows/android.yml`。release.yml から連結、
  手動実行では apk-preview プレリリースへ）。versionCode は package.json から自動導出
- 設定に「アプリについて」（バージョン表示・最新APKリンク）
- ユニットテスト: `pickVoice.test.ts` +8 / `capacitorTts.test.ts` +8 / `backup.test.ts` +6（全178件）

### 変更
- TTS層をエンジン差し替え式に再構成（`lib/tts/` — index / webSpeech / capacitorTts / pickVoice / types）。
  Web側の挙動・公開APIは不変
- バックグラウンド再生の窓口を `lib/backgroundSession.ts` に一本化（Web=キープアライブ / ネイティブ=FGS）
- 下部メニューのラベルを短縮（チャンク一覧→チャンク・例文一覧→例文）し折り返し禁止に、
  本文下部の余白を拡大（ネイティブのフォント拡大でもメニューが2行にならず、最後のボタンが隠れない）
- 🌙 暗くして再生と実験的キープアライブ設定は **Web限定表示** に（ネイティブでは画面オフ再生が標準のため不要）

### 備考
- ネイティブアプリの初回はデータが空です。PWAで「📦 フルバックアップ」→ アプリで「📥 復元」で移行してください

## [0.11.1] - 2026-07-14

v0.11.0 の実験的バックグラウンド再生が実機で「通知が出ない・画面オフで止まる」だった件の対策パッチ。

### 修正
- **キープアライブWAVを1秒→30秒ループに変更（振幅も0.01→0.02）**。Android Chrome は
  **5秒未満のメディアを「メディア再生」として扱わない**（メディア通知が出ず、バックグラウンド凍結の
  免除も受けられない）ため、1秒ループでは仕組みごと無効化されていた可能性が高い
- 連続再生の▶の下に**実験モードの作動状態**（🔋作動中 / ⚠開始できませんでした）を表示。
  実機で「キープアライブ自体が動いていない」のか「動いているが凍結は防げない」のかを切り分けられるように

## [0.11.0] - 2026-07-14

【実験的】画面オフでも連続再生の継続を試みるバックグラウンド再生。

### 追加
- **「🔋 画面オフでも再生を試す（実験的）」設定**（`lib/keepAlive.ts` + 設定の音声セクション、既定OFF）。
  連続再生・長文音読の間、ほぼ聞こえないキープアライブ音声（40Hz・-40dBFS・1秒ループを実行時生成）を
  流し続けてブラウザに「音を再生中のタブ」と認識させ、画面オフによるページ凍結（＝読み上げ連鎖の停止）の
  回避を狙う。※ 非公式なハックのため端末・Chrome のバージョンによっては効かない（その場合は従来どおり
  「🌙 暗くして再生」を使用）。iOS Safari では効果なし
- **Media Session 統合**。ON中は通知/ロック画面に再生中の教材（英文/和訳）を表示し、カード送りで更新。
  通知の⏸・イヤホン取り外し・他アプリの音声フォーカス奪取では再生全体を停止する
- ユニットテスト追加: `keepAlive.test.ts`（WAVヘッダ・非無音・振幅上限・ループ位相連続、計 +5件、全155件）

### 変更
- README を現在の姿（「貯める→練習する→使う→増やす」の学習サイクル完結型）に全面更新

## [0.10.0] - 2026-07-13

チャット練習まわりの使い勝手を改善。まとめが消えない・キーボードで会話が隠れない・無料枠の残りが見える。

### 追加
- **表現ストックに「✍️ 自分で追加」**（`ExpressionStock.tsx`）。思いついた表現を英語（＋任意で日本語）だけでその場でストックでき、
  あとから教材化（AI補完）でデッキへ。重複は正規化キーで検出して警告。例文まで自分で書く人向けに
  「チャンクを直接追加 →」（`/chunk/new`）への導線も設置
- **無料枠の残り目安表示**（`lib/usage.ts` + `components/UsageBadge.tsx`）。このアプリから送った
  APIリクエスト数を端末内で日次集計し（自動再試行分も計上）、既知モデルの無料枠 RPD 目安
  （Gemma 14,400 / Flash 250 など）との差分を「⚡ 残り 約n回」としてチャット練習・教材化・AI長文・設定に表示。
  集計日は無料枠のリセットに合わせ太平洋時間基準（日本時間の夕方ごろリセット）。
  ※ Gemini API は残クォータを返さないため、AI Studio や他端末での消費は含まれない目安
- ユニットテスト追加: `usage.test.ts`（太平洋時間の日付・RPD目安・日次カウント、計 +6件、全150件）

### 変更
- **チャットのまとめが他ページへ移動しても消えなくなった**。まとめ済みセッションも保持し、チャットへ戻ると
  まとめ画面をそのまま表示（新規セッションになるのはフォーカス指定起動と「もう一度」のみ）
- **チャット練習を全画面レイアウトに**（LINE風）。下部ナビを隠して入力バーを画面最下部へ置き、
  viewport の `interactive-widget=resizes-content` でソフトウェアキーボードの直上に入力バーが来るように。
  キーボード開閉時は最新メッセージまで自動スクロールし、AI側のトークが隠れない。
  ヘッダーの「‹」でホームへ戻れる（会話は保持）

## [0.9.0] - 2026-07-12

教材化への導線改善と、教材のアプリ内編集。「AI生成カナの手直し」「不要教材の整理」「思いついた表現の手動追加」が
Notion・CSV往復なしで完結する。

### 追加
- **チャンク編集**（`ChunkEdit.tsx` + `lib/phraseForm.ts`、`/chunk/:id/edit`）。チャンク詳細の「✏️ 編集」から、
  全フィールド（英語・訳・カナ・Type/Category/Level/Priority/ステータス/Note・例文最大5）を編集できる。
  **英文を変えてもID・SRS進捗・登録日時は維持**。カナは入力中にルール検証（kanaLint）を表示し、
  保存時に「カナ要確認」フラグを再計算（正しく直せば解除）。英文が空の例文行は保存時に除外
- **チャンク削除**（チャンク詳細の「🗑 削除」）。confirm（学習の記録も消える旨を明記）→ フレーズとSRS進捗を削除
- **チャンクの手動追加**（チャンク一覧の「＋ 追加」→ `/chunk/new`。同じフォームの新規モード）。
  既定値 Type=Chunk / Level=Core / 未着手。**同一英文の重複を正規化比較で防止**（重複時は既存チャンクへのリンクを表示）。
  追加分はアプリ内追加の教材としてマージ取り込みで保持される
- **ホームに「🗂 表現ストック」カード**（teal・件数バッジつき・0件でも常時表示）。これまで設定の奥にしか
  なかった表現ストック→教材化への導線がホームから1タップに
- ユニットテスト追加: `phraseForm.test.ts`（ラウンドトリップ・ID維持・空例文行の除外・カナ要確認の再計算/解除など、計 +11件、全144件）

### 変更
- チャンク詳細にステータスを表示し、カナ要確認バナーの案内を「✏️ 編集で修正すると解除」へ更新
- `useDeck` に `updatePhrase` / `removePhrase` を追加し、サンプル実体化ロジックを `ensurePersisted` に共通化（`addPhrases` と共用）
- `db.ts` に `deletePhrase` / `deleteProgress` を追加
- 教材化プレビューの入力欄 `Field` を `components/FormField.tsx` に切り出し（チャンク編集と共用）

## [0.8.1] - 2026-07-11

無料枠の混雑などで散発する Gemini API の 500 エラーへの耐性を改善するパッチ。

### 変更
- **モデルを選択式に変更**（`Settings.tsx`）。手入力欄をやめ、`Gemma 4（既定・無料枠が広め）` / `Gemini Flash（応答が速め）` / `その他（手入力）` から選べるようにした。混雑エラー（500）が続くときのモデル切り替えが1タップになる。プリセット外のモデル名を保存している場合は「その他」＋入力欄として表示（値は温存）
- **一時的な 5xx エラーの自動再試行**（`lib/chatApi.ts`）。`streamChat` が 5xx を受けたら 0.8秒 → 1.6秒 のバックオフを挟んで最大2回自動で再試行する（本文受信前なので重複なし・中止で中断可能）。チャット・教材化・AI長文の全AI機能に効く。429（レート上限）は上限消費を悪化させるため対象外
- 5xx のエラーメッセージを改善（生のレスポンスJSONの代わりに「AIサーバーで一時的なエラーが発生しました。混雑している可能性があります…」）

### 修正
- **教材化のAPIエラー時に再試行導線がなかった問題**（`StockEnrich.tsx`）。自動再試行でも回復しなかった場合、エラーバナーに「↻ 残りn件をもう一度生成する」ボタンを表示し、生成済み分を保持したまま続きから再開できる（最初のバッチで失敗してプレビューが空のまま行き止まりになるケースを解消）。キー無効（401/403）は設定の修正が必要なため再試行ボタンは出さない

## [0.8.0] - 2026-07-11

学習ループ「蓄積 → 教材化 → 練習 → AI会話 → 再蓄積」をアプリ内で完結させるリリース。
Notion・CSV・ファイル移動を挟まずに、チャットで出会った表現をそのまま教材にして練習を始められる。

### 追加
- **「教材化する」ボタン（表現ストック → AI補完 → デッキ直接追加）**（`StockEnrich.tsx`, `/stock/enrich`）。ストックした表現を選んで押すと、Gemma が不足項目（**和訳・平易な例文1件・シラブル音節カナ・Type/Category/Level**）を固定JSONスキーマで補完（`lib/enrich.ts`）。プレビューで全フィールドを確認・編集・除外してから「📚 デッキに追加」すると、IndexedDB のデッキに直接入り**すぐ練習に出てくる**（追加分はストックから自動削除、完了画面から「今日の練習へ」）。教材化＝即保存にはしない（AIの出力は下書き扱い）
  - コスト対策: ボタン起点のみ生成・デッキ収載済みは生成前に除外・和訳がある表現は訳を生成しない（given/MISSING指示＋アプリ側防御）・例文は1件固定・**5件ずつバッチ**・temperature 0.2・JSON解析失敗のリトライは1回。レート上限（429）時は成功分を保持して再試行できる
  - AI応答のJSON抽出は共通ユーティリティ `lib/aiJson.ts`（`<thought>`・コードフェンス・前後の散文を剥がして最初のJSONを厳格パース）
- **シラブルカナのルールベース検証 `lib/kanaLint.ts`**。AI生成カナの下書きを機械検査（記法にない文字・強勢`*`のペア・丸かっこ・区切り記号の位置＝error / 語数整合・強勢欠落＝warn）し、不合格は **`kanaWarnings`（CSV「カナ要確認」列と往復）フラグ付き**で保存（保存は妨げない）。教材化プレビューで理由を表示・編集のたび即時再検証、チャンク詳細に「⚠ カナ要確認」バッジ。運用（CSVで書き出して ChatGPT/Claude で定期監査）は `docs/シラブル音節記法.md` §5
- **AIで長文を作る**（`LongReadingCreate.tsx`, `/long-reading/new`、`lib/longGen.ts`）。テーマ（チップ候補つき自由入力）・難易度（Basic/Core/Advanced）・長さ（1段落/2段落）と、デッキから選んだ**チャンク3〜5個**（SRS期日到来分を先頭提示・検索可）を指定すると、それらを自然に織り込んだ読み物（英文＋和訳）を生成。プレビューで編集でき、指定チャンクの**含有チェック**（✓/✗チップ、`matchesChunk` 再利用）と再生成つき。保存すると `Type='Long Reading'` としてデッキに追加され、そのまま長文音読へ（カナなし）。導線は長文音読のヘッダー「＋ AIで作る」と空状態
- **CSVエクスポート/共有**（`lib/export.ts` / `lib/share.ts`）。設定「📤 デッキをCSVでバックアップ（n件）」は**ステータス・カナ・カナ要確認まで含む全列**を出力し、再取り込みで完全復元できる（ラウンドトリップを `export.test.ts` で担保。BOM＋CRLF＋RFC-4180）。表現ストックにも「📤 CSVで共有 / 保存」。書き出しは **OSの共有シート（`navigator.share`）優先**・非対応環境はダウンロード
- ユニットテスト追加: `export.test.ts` / `merge.test.ts` / `kanaLint.test.ts` / `enrich.test.ts` / `longGen.test.ts`（計 +51件、全129件）

### 変更
- **CSVインポートを「全置換」から「マージ」既定に変更**（`mergePhrases`）。ID一致は上書き・**アプリ内で追加した教材は保持**・削除は伝播しない（`createdTime`/`kanaWarnings` の温存規則つき）。結果は「追加 x件・更新 y件（既存 z件は保持）」と表示。Notion側の削除を反映したいときは設定の「全置換」チェックで従来動作
- `useDeck` に `addPhrases`（アプリ内追加。**サンプル使用中は先にサンプルを実体化**してから追記し、データが消えて見えないようにする）、`db.ts` に `putPhrases`（clearしないupsert）を追加
- 「取り込みデータを消去」の確認文言を更新（アプリ内追加分も消える旨＋事前バックアップの案内）
- 仕様書 v1.5 / 要件定義書 v1.9（教材化 §4.20・AI長文 §4.21・CSVバックアップ FR-06b/FR-175・マージ取り込み FR-06・カナ要確認 FR-179・NFR-08 ほか）

## [0.7.0] - 2026-07-10

### 追加
- **チャット練習モード**（`ChatPractice.tsx`, `/chat`）。AI英語コーチと英語で会話しながら、SRSで選んだ対象チャンク（設定で2〜6個）を実際に使う練習。コーチは基礎語彙・1文5〜6語程度の短文・シンプルな構造・日常/ビジネスで使い回せるフレーズを重視し、間違いには `✏️` 行でより自然な言い方＋日本語解説を添える。対象チャンクはチップ表示され、発話で使えると点灯（正規化＋単語境界マッチ、穴あきチャンク `It takes ... to ~` 対応。クライアント側検出で `lib/chunkMatch.ts`）。終了時は日本語まとめ（使えたチャンク数・アドバイス）を表示し、✉️ メール共有（mailto）と 📋 コピーができる
  - **バックエンドは Gemini API の OpenAI 互換エンドポイント**（無料枠。既定モデル `gemma-4-31b-it`、設定で変更可）。SDK なしの最小クライアント `lib/chatApi.ts`（fetch + SSE ストリーミング、401/429/オフラインのエラーマッピング、Gemma 系が `system` ロールを 400 で拒否した場合は user へ前置してリトライ、Gemma 4 の内部思考 `<thought>…</thought>` は表示・履歴から除去）。APIキーは設定画面から入力し端末内（localStorage）にのみ保存
  - **導線**: ホームのモードボタン、下部メニューの「💬 チャット」タブ（タブ移動しても会話は保持・再開）、チャンク詳細「💬 このチャンクで会話練習」・例文カード「💬 この例文で会話練習」（`?focus=<id>(&example=<n>)` でそのチャンク・例文の場面を中心に会話を組む）
  - **エラー時も履歴を保持**して再送可能。日本語IMEの変換確定 Enter では送信しない（`isComposing` ガード）
- **追加候補チャンクの提案と「表現ストック」**（`ExpressionStock.tsx`, `/stock`）。まとめの最後にコーチが「会話に出てきた・役立ったはずの、フレーズ集未収載の基本表現」を `➕ 英語 — 日本語訳` 形式で提案（`lib/suggestions.ts` がパースし、デッキ収載済みは正規化一致で自動除外）。チェックボックスで選ぶと端末内に日付付きで蓄積され、数日分たまったら表現ストック画面からタブ区切りコピー（表計算/Notionテーブルに直貼り）やメール送信で PC へ渡して例文作成 → Notion 追加する運用を想定。個別削除・全削除つき。導線はまとめのリンクと設定画面
- **設定に「チャット練習（AI）」セクション**。APIキー（password型）・モデル名・解説の日本語切替・対象チャンク数、Google AI Studio へのリンク、表現ストックへのリンク
- ユニットテスト追加: `chatApi.test.ts`（SSEパース・エラーマッピング・systemロールfallback・`stripThoughts`）、`chunkMatch.test.ts`、`suggestions.test.ts`（計 +33件）

## [0.6.4] - 2026-07-06

### 変更
- **読み上げハイライトの機能名を「Word Spark」に統一**（旧称: カラオケ式ハイライト）。コード内コメント・docs（仕様書 §9.1・要件定義書 §4.16）・CHANGELOG の表記を一括変更（UI 文言・動作に変更なし）
- **Word Spark の推定タイミングを改善**（`boundary` イベントが発火しないエンジン向けフォールバック。`lib/spokenWords.ts`, `useSpokenWordTracker`）
  - **速度の自動較正**: 実際の発話時間（`onstart`→`onend` の実測）から端末×声×再生速度ごとの文字/秒を学習し `localStorage` に保存。従来の固定 15文字/秒 × rate の仮定による「ハイライトが音声より速い/遅い」ズレが、数回の再生で実速度に収束する。途中キャンセルなどの外れ値は捨て、指数移動平均でならす
  - **英語の強勢リズムを近似**: 単語の重みを文字数比例 → 音節数推定（母音グループ）ベースに変更。機能語（冠詞・前置詞・代名詞・助動詞など弱形で読まれる語）は短く、内容語・多音節語は長く。カンマ・文末句読点の後にはポーズを置き、均等な機械的リズムだった単語送りに強弱をつけた
  - **開始時刻のズレを解消**: 推定タイマーの起点を speak() 呼び出し時 → 実際の発話開始（`onstart`）に変更（連打時のキャンセル待ち 120ms やエンジンの起動遅延ぶん先走っていた）

## [0.6.3] - 2026-07-04

### 追加
- **例文カード**（`ExampleDetail.tsx`, `/example/:phraseId/:index`）。例文一覧の行タップで、その例文を1枚のカードで練習する画面へ遷移（瞬間英作文の例文表示と同じ「日本語→タッチ→英文＋カナ＋Word Spark ハイライト」、共通 `ReproCard`）。「← 戻る / 進む →」で絞り込み結果内の例文を移動でき、チャンク詳細への導線も持つ（旧: 行タップで親チャンクの詳細へ）
- **チャンク詳細・今日の練習の5例文表示にインライン Word Spark**（`ExampleList`）。各例文の🔊を押すと、5例文を表示したまま**その行だけ**読み上げに合わせて単語ハイライトが進む。再生中に別の例文の🔊を押すとハイライトが移る（世代トークンでキャンセル済み発話の終了イベントを無視）

### 修正
- 再現練習カード（`ReproCard`）で「🔊もう一度」を再生中に連打すると、キャンセルされた旧発話の終了イベントが新しい Word Spark ハイライトを消すことがある競合を修正（同じ世代トークン方式）

## [0.6.2] - 2026-07-04

### 追加
- **Word Spark 読み上げハイライト**（リリース当時の名称: カラオケ式読み上げハイライト）。読み上げ中の単語をマーカー風（背景アンバー）に強調し、音の現在地を目で追えるように。対象は**長文音読の本文**と**再現練習カード（`ReproCard`）の開示英文**（瞬間英作文・今日の練習・フレーズ再生の再現練習で共通）。Web Speech API の `boundary` イベント対応エンジン（PC Chrome・iOS Safari 等）では実際の発話に同期し、発火しないエンジン（Android Chrome の Google TTS 等）では文字数×再生速度の**推定タイミング**で単語を送る（`lib/spokenWords.ts` 純関数＋`useSpokenWordTracker` フック＋`SpokenText` コンポーネント、`tts.ts` に `onBoundary` オプション追加）。文字サイズは変えず行の折り返しが揺れない設計

## [0.6.1] - 2026-07-04

### 追加
- **例文一覧（`Examples.tsx`）の各行に音節カナ表示**を追加（英文の下に `KanaLine`。設定「カナ表示」ON かつ `Example.kana` があるときのみ、強勢は太字）。チャンク一覧の行は一覧性維持のため引き続き非表示

### 変更
- **チャンク一覧（`Browse.tsx`）にページングを追加**（1ページ100件、例文一覧と同じ前後送りUI）。フィルタ変更時は先頭ページへ戻す。ページ送りUIは共通コンポーネント `Pager.tsx` として例文一覧と共有
- **例文一覧（`Examples.tsx`）の1ページ表示件数を 30 → 100 件**に変更

### 削除
- **発音練習モード**（`Pronounce.tsx` / `ListenPractice.tsx`, `/pronounce`, ホーム導線）。チャンク詳細（`ModelCard` のチャンク＋5例文・個別再生）と連続再生で同等のことができ機能が重複していたため。お手本を聞いて反復するモデリング形式は「今日の練習」の新規カード向けに継続

### 修正
- **CI: GitHub Pages デプロイの `npm ci` 失敗を修正**。vitest 4.x が vite 5 プロジェクトに別系統の vite/esbuild を持ち込み、`package-lock.json` に一部プラットフォームの `@esbuild/*` が記録されず `npm ci` が lock 不整合で失敗していた。vitest を vite 5 と依存を共有する `^2.1.9` に固定し、lock を再生成して解消（アプリの挙動・バージョンに変更なし）

## [0.6.0] - 2026-07-04

### 追加
- **「今日の練習」統合フロー**（`Daily.tsx`, `/daily`）。SRSの期日が来たカードを、習熟度（Leitnerボックス）に応じた練習形式で自動出題する: 新規=モデリング → box 0-1=再現練習 → box 2-3=瞬間英作文 → box 4以上=文脈穴埋め（穴埋め不可のチャンクは瞬間英作文のまま）。形式は表示のたびに進捗から算出（`lib/dailyForm.ts` の `formFor`）するため、採点で box が動けば同一セッション内の再出題でも形式が追従する。期日到来カードが0件のときは完了表示（`useSession` に `noFallback` オプションを追加、既存モードのランダム出題は従来どおり）。ホーム統計直下に「▶ 今日の練習（期日件数つき）」の最上位導線を追加
- **チャンク詳細画面**（`ChunkDetail.tsx`, `/chunk/:id`）。英語・カナ・日本語・メタ情報・note・例文一覧（個別再生つき）に加え、学習の記録（習熟度 box/5・できた/できなかった回数・次回出題日・「覚えた」トグル）を表示。連続再生（フレーズ再生）へは画面下部の「▶ 連続再生」から明示的に遷移し、一覧から受け取った絞り込み結果（`ids`）と戻り先（`backTo`）をそのまま引き継ぐ
- **文脈穴埋めモード**（`Cloze.tsx`, `/cloze`）。例文中のチャンクを伏せ字（`____`）にして、文脈と日本語ヒントからチャンクを想起する練習。マッチングは大文字小文字無視＋単語境界チェックのみ（活用形は対象外、`lib/cloze.ts` の純関数群）。チャンクが見つかる例文を持つフレーズだけを出題し、1チャンクにつきマッチ例文からランダムに1つを出す。ホームに「🧩 文脈穴埋め」導線を追加
- **ユニットテスト基盤**（vitest, `npm test`）。`lib/cloze.test.ts`（マッチング・伏せ字生成）と `lib/srs.test.ts`（採点による box 遷移・期日計算）を追加

### 変更
- **チャンク一覧・例文一覧の行タップ先をチャンク詳細（`/chunk/:id`）に変更**。連続再生へ直接入る従来のワンタップ導線は、チャンク一覧の各行に追加した「▶」ボタンで維持（🔊単発再生は従来どおり）
- 瞬間英作文の採点ボタンとカード表示を `GradeButtons.tsx` / `ComposeCard.tsx` に、モデリング/発音練習のカード表示を `ModelCard.tsx` に部品化（「今日の練習」と共有するため）。`ReproCard` に進行状態を親へ通知する任意 prop `onStep` を追加（既存利用箇所は無変更）
- **瞬間英作文を逐次練習型に刷新**。従来の「チャンク和訳→開示→例文一覧」をやめ、**「日本語訳→タッチで答え合わせ」をチャンク→例文1→…例文5と1枚のカードで続けて練習**する形式に（共通 `ReproCard`＋`chunkAndExampleItems`。和訳のない例文はスキップ）。最後の項目まで答え合わせすると3段階採点が出る。「今日の練習」の compose 形式（box 2-3）も同じ挙動
- **再現練習カード（`ReproCard`）の開示英文に「🔊 もう一度」再生ボタン**を追加。答え合わせ後に何度でも聞き直せる（瞬間英作文・今日の練習・フレーズ再生の再現練習で共通。カード送りには伝播しない）
- **瞬間英作文の出題順を Type → Category でクラスタ化**（`clusterByTypeCategory` / `useSession` の `clusterByFacet`）。次のチャンクは同じタイプ・同じカテゴリを優先し、表現のまとまりで練習できる（グループはSRSの弱い順の初出順、グループ内も弱い順を維持）
- モデリング/発音練習のカード表示から例文一覧を `ExampleList.tsx` として部品化（ModelCard・チャンク詳細で共用。音声は🔊を押した例文のみ再生）
- **発音練習・今日の練習（model形式）の自動読み上げをチャンクのみに変更**。従来はカード表示のたびにチャンク＋5例文を連続で自動再生していたが、例文までの連続読み上げは「🔊お手本を聞く」を押したときだけに。**お手本再生中は「⏸停止」トグル**でいつでも止められる

### 削除
- **モデリングモード**（`Modeling.tsx`, `/modeling`, ホーム導線）。発音練習と実装・目的が重複していたため統合（発音練習＝チャンク＋5例文をお手本に音読、シャッフル/自信なし絞り込みつき）。「今日の練習」の新規カード向け**モデリング形式**（`ModelCard` によるお手本音読）は継続

## [0.5.0] - 2026-06-29

### 追加
- **長文音読モード**（`LongReading.tsx`, `/long-reading`）。`Type = Long Reading` の長文だけを対象に、本文（`examples[0]`）をお手本として読み上げ、続けて音読する。本文領域はスクロール、訳の表示切替・前後送り（`StepNav`）に対応。再生中は Wake Lock でスリープ抑止。ホームに「📖 長文音読」導線を追加。長文（Long Reading）は他モード・各一覧・ホーム集計から除外（`excludeLongReading`）
- **シラブル音節カナ＋強勢表示**（`KanaLine.tsx`）。チャンク／例文の英文の下に、アメリカ英語ベースの音節カタカナを小さく表示。強勢は `*…*` で囲むと太字＋濃色で描画。CSVに任意列 `音節`（チャンク）/ `音節1〜音節5`（例文）で投入し、`Phrase.kana` / `Example.kana` に格納（任意・後方互換）。表示はカード/詳細/練習画面のみ（チャンク一覧・例文一覧は対象外）。設定に「カナ（音節）を表示」トグル（既定ON）。記法は `docs/シラブル音節記法.md` に規定（`・`音節区切り / `‿`リンキング / `( )`脱落音 / `*…*`強勢）

### 変更
- **再現練習（`ReproCard`）の自動送りを廃止**。英文表示後に2秒で自動遷移していたのをやめ、
  「和訳 → タッチ → 英文 → タッチ → 次の項目」と各ステップをタッチで進める方式に変更（瞬間英作文・フレーズ再生の両方）
- 各練習画面の **離脱導線（「✕ やめる」「← 一覧へ」）を上部ヘッダーから画面下部へ移動**し、片手で操作が完結できるように統一
  （瞬間英作文・発音練習・モデリング・長文音読・フレーズ再生）。上部ヘッダーはタイトルと進捗のみ

## [0.4.0] - 2026-06-27

### 追加
- 下部メニューに **「例文一覧」タブ**（`Examples.tsx`）。全チャンクの例文1〜5を平坦に展開し、英文＋日本語訳をページング表示（1ページ30件）。行タップで親チャンクのフレーズ再生へ遷移、各例文の単発TTS再生にも対応
- favicon・ホーム画面アイコン（PWA / apple-touch-icon）を追加

### 変更
- 下部メニューの「一覧」を **「チャンク一覧」** に改称
- チャンク一覧・例文一覧で上部フィルタ（検索／自信なし／ファセット）を共通フック `usePhraseFilter` ＋ 共通UI `FacetFilters` として共有
- フレーズ再生の「← 一覧へ」が起動元の一覧（チャンク／例文）に応じて戻り先を切替（`location.state.backTo`）

## [0.3.1] - 2026-06-26

### 変更
- 瞬間英作文の「例文を見る」を、フレーズ再生の再現練習（日本語→タッチ→英文→2秒後に次へ／英文はタッチまで非表示）と同一挙動に統一。両画面で共通コンポーネント `ReproCard` を使用

## [0.3.0] - 2026-06-26

### 追加
- **チャンク + 例文最大5 + メタ情報**のデータモデルへ刷新（`Phrase.examples:{en,ja}[]` と `type/category/level/priority/note`）
- 一覧に **Type / Category / Level / Priority のファセット絞り込み**（値はデータから動的生成）。検索は例文（英・和）も対象
- 連続再生で **チャンク→例文1→… の数珠つなぎ**再生と、各例文の和訳表示
- 再生の **間（ま）**: 1項目内（和訳↔英文）1.5秒 / 次の項目へ 2秒
- 日本語訳ON時の **「和訳→英文」順**再生
- **再現練習**: 通常時（自動再生・暗くして再生以外）かつ日本語訳ONで、日本語→タッチ→英文の答え合わせ（英文はタッチまで非表示）
- **CSV直接インポート**（新ヘッダー: `Example{n}`/`日本語訳{n}` の交互列）。設定画面でCSV/Notionの2案内
- 共通TTSヘルパ `speakSequence`（パート単位の間）と `MetaChips` コンポーネント

### 変更
- 練習3モードを再定義（瞬間英作文=チャンクJP→EN+例文ネットワーク、モデリング/発音練習=チャンク+5例文の音読）。全チャンクを対象に
- インポートは `ID` 列をそのまま id に採用（旧Notion形式は後方互換で継続対応）
- サンプルデータを新形式（和訳入り）へ更新

### 削除
- 文数による瞬間英作文/モデリング振り分け（`src/lib/text.ts`）

### 注意
- id 採番方式の変更（FNVハッシュ→`ID`列）に伴い、旧データからの移行で既存のSRS進捗・「覚えた」は一度リセットされる

## [0.2.0] - 2026-06-20

### 追加
- **モデリング練習**と**フレーズ別の発音練習ビュー**（`ListenPractice` を共通化して再利用）
- **連続再生プレイヤー**（`src/pages/PhraseDetail.tsx`）: 一覧でタップしたフレーズを起点に、
  フレーズ / 例文 / 日本語訳 の読み上げ項目を切り替えながら自動で次々再生。リピート / シャッフル対応
- 3カラム（フレーズ・日本語訳・例文）対応
- 「**覚えた**」チェックと「**自信なし**」絞り込み
- 連続再生中の **Screen Wake Lock**（画面消灯で読み上げが止まらないように画面スリープを抑止）
- **「🌙 暗くして再生」モード**: 全画面を黒く覆い、Wake Lock で読み上げを継続したまま誤タッチを無効化。
  1本指タッチは無効、2本指同時タッチで解除（バックライト自体は Web から消せないため見た目を黒くするのみ）

### 変更
- 読み上げ速度の調整は**英語のみ**対象に（日本語訳は常に等倍で再生）

## [0.1.0] - 2026-06-17

### 追加
- My Phrases 初版（瞬間英作文 / 発音練習の PWA、React + Vite + TS + Tailwind）
- Notion の Markdown & CSV エクスポートをアプリ内インポート（IndexedDB 保存・オフライン対応）
- SRS（Leitner ボックス）による学習状態管理
- Web Speech API による読み上げ、GitHub Pages への自動デプロイ

[Unreleased]: https://github.com/yuna5963/My-Phrases/compare/v0.4.0...HEAD
[0.4.0]: https://github.com/yuna5963/My-Phrases/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/yuna5963/My-Phrases/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/yuna5963/My-Phrases/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/yuna5963/My-Phrases/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/yuna5963/My-Phrases/releases/tag/v0.1.0
